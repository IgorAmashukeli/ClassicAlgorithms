# ClassicAlgorithms

Classic Algorithms, implemented in C++

To run each task, do:

`mkdir build`
`cd build`
`cmake ..`
`cmake --build .`

And then run all the created executables

(

example for /Trees/0_BST:
`./set_tests && ./map_tests`

)
